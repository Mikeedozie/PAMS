# Docker Compose env (example). Copy to .env.docker and set real values. Do NOT commit .env.docker

# Database
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql+psycopg2://pams_user:change-me@postgres:5432/pams

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Grafana
GF_SECURITY_ADMIN_PASSWORD=change-me
